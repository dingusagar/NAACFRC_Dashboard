dash==2.9.3
pandas==2.2.2
numpy==1.26.4
plotly==5.20.0
geopandas==0.13.2
shapely==2.1.3
fiona==1.9.4
pyproj==3.7.0
rtree==1.0.1
"dash[compress]"

# Note: Installing geopandas and its binary deps (fiona, gdal, rtree, pyproj) 
# can be platform-specific. On macOS it's recommended to use a conda environment
# (mamba/conda) or brew packages to install GDAL/PROJ first, or use the wheels
# provided by PyPI where available.
